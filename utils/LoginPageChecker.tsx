// LoginPageChecker.ts
const LoginPageChecker = (username: string, password: string): boolean => {
    // Check if username and password match the expected values
    return username === "abc" && password === "123";
  };
  
  export default LoginPageChecker;
  